<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/windows-powershell/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Windows PowerShell — raw notes

## Target / environment
- VM/attack box used:
- PowerShell version (5.x vs 7.x) if you noticed:

## Getting oriented: help and command discovery
- Actual `Get-Help`, `Get-Command`, `Get-Alias`/`Get-Verb` usage and what came back:

## Navigating the file system
- Actual `Get-ChildItem`/`Set-Location`/`Get-Content`/`New-Item` (etc.) commands you ran:
- Did you explore any PSDrives (`HKLM:`, `Env:`, `Cert:`)?

## The pipeline and object filtering
- Actual pipeline you built (`Where-Object`, `Sort-Object`, `Select-Object`, `Format-Table`, etc.):
- What it was filtering/sorting and what the result looked like:

## System and network information
- Actual `Get-Service`, `Get-Process`, `Get-NetIPAddress`, `Get-CimInstance` output you got:

## Execution policy
- Did the room have you check/change it? What was it set to, what did you set it to:

## Variables and scripting
- Did you write a `.ps1` script or one-liner? What did it do:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
