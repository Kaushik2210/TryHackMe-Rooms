<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/linux-fundamentals-part-2/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# Linux Fundamentals Part 2 — raw notes

## Target / environment
- Target IP/hostname, and how you connected (`ssh user@ip`, port, key vs. password):

## SSH and file transfer
- Actual `ssh` command(s) you ran, including any non-default port or key file:
- Did you use `scp`/`sftp`? What did you move, in which direction?

## Manual pages and flags
- Any command where you actually checked `man` to figure out a flag?

## File and directory manipulation
- `cp -r`, `mv`, `rm -r`, `mkdir -p`, `nano`/`vim` — what did you actually create/edit/delete?
- Did you use `wc -l` or `file` on anything specific?

## Permissions
- What did `ls -l` show for the files you worked with?
- Actual `chmod` (symbolic and/or octal) and `chown`/`chgrp` commands you ran, and the before/after permissions:

## Finding files
- Any `find` or `locate` commands — what were you searching for, and what turned up? (e.g. SUID files, backups)

## Disk usage
- Did you run `df -h` / `du -sh` on anything? What did you find (or were told to find)?

## Symbolic / hard links
- Did you create a symlink or hard link? What was it for?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
