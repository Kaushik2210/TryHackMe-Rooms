<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/windows-cli-basics/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Windows CLI Basics — raw notes

## Target / environment
- VM/attack box used:
- Shell used (cmd.exe, or did the room also touch PowerShell):

## Opening the terminal / first impressions
- How you launched it, what the prompt looked like:

## Navigating the file system
- Actual `dir`/`cd`/`tree` commands you ran and what came back:
- Any drive-switching (e.g. `D:`) you had to do:

## File and directory operations
- Actual `mkdir`/`copy`/`del`/`type`/`echo` commands you ran:
- What you created/moved/deleted, and what the output looked like:

## System information commands
- What `whoami`, `hostname`, `ipconfig` (etc.) returned for you:

## Getting help / productivity habits
- Did you use `/?`, tab completion, `F7` history, `cls`? Anything that saved you time:

## Wildcards
- Any wildcard usage (`*`, `?`) and what it matched:

## Flags / task answers
- Any specific answers you had to submit (I'll redact flag-style values):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
