<!--
Fill this in from memory / your terminal or CyberChef history. Rough bullets are fine — I turn this
into full prose. The more specific you are (actual commands, actual hashes/ciphertext, actual wrong
turns), the better and more defensible the final article. Delete any section that doesn't apply.

Drop any screenshots you have into this same folder (_incoming/cryptography-concepts/) —
filenames don't matter. No screenshots is fine too.
-->

# Cryptography Concepts — raw notes

## Target / environment
- Tools used (CyberChef, openssl, hashcat/john, a THM-provided terminal, other):

## Symmetric vs. asymmetric encryption
- Any exercise identifying/using AES, RSA, or a mode of operation (CBC vs GCM)? What did you actually do?
- Commands/tools used and what happened:

## Hashing
- Any exercise hashing data or identifying a hash type (SHA-256, MD5, bcrypt, etc.)?
- Actual commands/output:
- Password hashing vs general hashing — did the room have you compare fast vs. slow hash algorithms directly?

## Digital signatures / TLS handshake
- Did the room walk through a signature verification exercise, or a TLS handshake walkthrough/capture?
- What you actually did / observed:

## Wrong turns
- Anything you tried that didn't pan out (wrong hash type guessed, wrong tool, wrong mode):

## Flags / answers
- Any flags you captured (I'll redact the actual values — just note where/how you got each one):
- Any other task answers worth remembering:

## What took longest / what you'd do differently
-

## Anything else worth noting
-
