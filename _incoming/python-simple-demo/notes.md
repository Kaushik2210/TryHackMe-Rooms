<!--
Fill this in from memory / your terminal or editor history. Rough bullets are fine — I turn this
into full prose. The more specific you are (actual code, actual output, actual wrong turns), the
better and more defensible the final article. Delete any section that doesn't apply. Don't worry
about grammar or completeness; I'll ask follow-up questions for anything that's too thin to write
from.

Drop any screenshots you have into this same folder (_incoming/python-simple-demo/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Python: Simple Demo — raw notes

## Environment
- Where did you run the Python (room's built-in terminal, local `python3`, a specific version)?

## Variables and types
- Any actual variables/types you played with, or `type()` checks you ran?

## Strings / f-strings
- Any real string formatting you did — building a URL, a message, output for a script?

## Control flow
- Any `if`/`elif`/`else` or `for` loop code you wrote — what was it deciding/iterating over?

## Functions, lists, dictionaries
- Any function you wrote, list comprehension, or dict lookup — actual code and what it produced?

## File I/O and modules
- Did you read/write a file with `open()`/`with`? What file, what content?
- Did you use `sys.argv` or import anything else?

## Exceptions
- Did you write or trigger a `try`/`except`? What input caused the exception, and what happened?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
