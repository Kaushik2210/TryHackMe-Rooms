<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/windows-command-line/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Windows Command Line — raw notes

## Target / environment
- VM/attack box used:
- Elevated (Run as Administrator) or standard prompt:

## File and directory manipulation
- Actual `dir /a`, `dir /s`, `attrib`, `xcopy`/`robocopy`, `findstr`, `fc` commands you ran:
- What they turned up:

## Process and service inspection
- Actual `tasklist` / `taskkill` / `sc query` / `net start`/`net stop` commands you ran:
- What was running, anything you killed or (re)started:

## User and network administration
- Actual `net user`, `net localgroup`, `net share`, `ipconfig /all`, `netstat -ano`, `nslookup` output:
- Anything notable about accounts, shares, or connections you found:

## Command chaining and redirection
- Any use of `&`, `&&`, `||`, `|`, or `>`/`>>`/`2>&1` — what you were trying to do:

## Batch scripting
- Did the room have you write/run a `.bat` file? What did it do:

## Environment variables / PATH
- Anything you looked at with `set` or `%PATH%`:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
