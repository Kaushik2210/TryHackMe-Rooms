<!--
Fill this in from memory. Rough bullets are fine — I turn this into full prose. This room is
concept/quiz-style rather than hands-on exploitation, so the notes lean toward what you learned and
how you'd apply it, not commands/output. Delete any section that doesn't apply.

Drop any screenshots you have into this same folder (_incoming/client-server-basics/) —
filenames don't matter. No screenshots is fine too.
-->

# Client-Server Basics — raw notes

## What the room actually covered (in your own words)
- The request-response model — what example did the room use (browser/web server, DNS, SSH, other)?
- Ports, sockets, and addressing — did the room have you look at specific port numbers or a socket example?
- Client-server vs peer-to-peer — what example did it use for P2P (BitTorrent or other)?
- Stateless vs stateful protocols — did the room walk through cookies/sessions as the mechanism HTTP uses to fake state?
- The worked HTTP request/response example — did the room have you read or construct raw HTTP headers?

## Anything the room got you thinking about beyond the textbook definition
- e.g. a time you've seen client-side validation bypassed, how this maps to something you already knew about web requests:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## Where this connects to other rooms you've done
- e.g. does this tie into Inside a Computer System, Computer Types, Data Encoding, Virtualisation Basics?

## What took longest / what surprised you
-

## Anything else worth noting
-
