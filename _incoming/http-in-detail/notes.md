<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/http-in-detail/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# HTTP in Detail — raw notes

## Target / environment
- Target URL/host (if any lab machine was involved):
- Attack box / tool used (curl, browser DevTools, Burp, etc.):

## Requests/responses inspected
- Tools used (curl -v, browser Network tab, etc.) and the actual command(s) you ran:
- What method(s) did you send (GET, POST, etc.) and to what endpoint?
- What headers/status codes came back that you noticed:
- Any security headers you specifically looked for (CSP, HSTS, Set-Cookie flags, CORS headers)?

## What you found
- Anything that stood out (missing security header, verbose error, interesting redirect, etc.):
- Any wrong turns — things you tried that didn't pan out:

## Flags / answers
- Any flags you captured (I'll redact the actual values in the published article — just note
  where/how you got each one):
- Any other task answers worth remembering:

## What took longest / what you'd do differently
-

## Anything else worth noting
-
