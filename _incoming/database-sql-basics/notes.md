<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual queries, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/database-sql-basics/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Database SQL Basics — raw notes

## Environment
- What database/client did you actually use (MySQL CLI, `sqlite3`, a web-based SQL console in the room)?
- What tables/schema did the room give you to work with?

## SELECT and WHERE
- Actual `SELECT ... WHERE` queries you ran, and what came back:
- Any comparison/pattern-matching (`LIKE`, `IN`) you used?

## JOIN
- Any `JOIN` queries you wrote — which tables, and what was the result?
- Did you need `LEFT JOIN` vs `INNER JOIN` at any point, and why?

## Aggregation
- Any `GROUP BY`/`COUNT`/`SUM`/`ORDER BY` queries you ran — what were you summarizing?

## SQL injection (if the room covered it hands-on)
- Did you actually try an injection payload (e.g. `' OR '1'='1`) against a login form or query? What happened?
- Did you see or write a parameterized-query fix?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
