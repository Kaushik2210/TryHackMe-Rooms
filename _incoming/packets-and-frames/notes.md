<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. This room is mostly concept-driven, but if it had you look at real frames/packets (e.g. in
Wireshark) note that under Hands-on below — otherwise skip that section. Delete any section that
doesn't apply.

Drop any screenshots you have into this same folder (_incoming/packets-and-frames/) — filenames
don't matter. No screenshots is fine too.
-->

# Packets & Frames — raw notes

## What the room actually covered (in your own words)
- Encapsulation and the PDU-per-layer terminology (data/segment/packet/frame) — how did the room lay
  this out?
- Ethernet frame structure (dest MAC, src MAC, EtherType, payload, FCS) — did the room show a real
  frame breakdown?
- MTU and fragmentation — what example did the room use?
- IPv4 header fields (TTL, Protocol, checksum, etc.) — which fields did the room focus on?
- Frames vs packets — how did the room explain that MAC addresses change hop-by-hop but IP addresses
  stay constant?

## Hands-on (if the room had you use Wireshark, tcpdump, or similar)
- Tool used and what you looked at:
- What fields/filters you actually inspected (e.g. eth.addr, ip.ttl):
- Anything that stood out in the capture:

## Anything the room got you thinking about beyond the textbook definition
- e.g. OS fingerprinting via TTL, fragmentation-based IDS evasion, or a covert-channel idea:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can
  stay visible in the final article):

## Where this connects to other rooms you've done
- e.g. does this tie into OSI Model, Intro to LAN, Networking Core Protocols?

## What took longest / what surprised you
-

## Anything else worth noting
-
