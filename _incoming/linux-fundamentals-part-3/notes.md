<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/linux-fundamentals-part-3/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# Linux Fundamentals Part 3 — raw notes

## Target / environment
- Attack box or lab machine used:

## Text processing pipelines
- Any real `grep`/`cut`/`sort`/`uniq`/`sed`/`awk` pipelines you built — actual command lines and what they produced:
- What log or text file were you working against?

## Package management
- Any `apt`/`dpkg` (or other package manager) commands you actually ran — install/remove/update:

## Cron
- Did you view or edit a crontab (`crontab -l`/`crontab -e`)? What job did you look at or create?
- Any system-wide cron files (`/etc/crontab`, `/etc/cron.d/`) you inspected?

## Logging
- What did you check in `/var/log`? Any `journalctl` usage — which unit, what filters?

## Process management / services
- Any `ps`/`top`/`kill`/`systemctl` commands you ran — what process/service, and what happened?

## Archiving as automation
- Did you write or run a backup-style `tar` + `find ... -delete` pattern? What was it for?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
