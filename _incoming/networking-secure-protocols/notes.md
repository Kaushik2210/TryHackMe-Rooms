<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/networking-secure-protocols/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# Networking Secure Protocols — raw notes

## Target / environment
- Target IP or hostname (if any lab machine was involved):
- Attack box used (TryHackMe AttackBox / own Kali / other):

## TLS/SSL
- Tools used (openssl s_client, browser devtools, etc.) and the actual command(s) you ran:
- What you inspected (cipher suite negotiated, certificate chain, TLS version):
- Anything that stood out (deprecated version offered, self-signed cert, etc.):

## SSH
- Commands you ran (ssh, ssh-keygen, connecting to a lab host):
- What happened (host key prompt, key-based vs password auth, any port-forwarding):

## VPN (IPsec / WireGuard) — if the room had any hands-on here
- What you set up or inspected:
- Result:

## Wrong turns
- Anything you tried that didn't pan out, or a config/flag that confused you at first:

## Flags / answers
- Any flags you captured (I'll redact the actual values in the published article — just note
  where/how you got each one):
- Any other task answers worth remembering:

## What took longest / what you'd do differently
-

## Anything else worth noting
-
