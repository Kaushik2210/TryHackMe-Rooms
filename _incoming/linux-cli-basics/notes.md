<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/linux-cli-basics/) — filenames
don't matter, I'll rename them. No screenshots is fine too.
-->

# Linux CLI Basics — raw notes

## Target / environment
- Attack box or terminal used (TryHackMe browser terminal, AttackBox, own machine):

## Navigation
- What directories did you actually move through (`pwd`, `ls -la`, `cd`)? Anything unexpected in `ls -la` output?
- Absolute vs. relative paths — any mix-up worth mentioning?

## File inspection and manipulation
- What files did you `cat`/`less`/`head`/`tail`? What were you looking for?
- Any `cp`/`mv`/`rm`/`mkdir`/`touch` you ran, and why:
- Redirection (`>`, `>>`) or piping (`|`) you actually used — real command lines:

## Searching (grep / find)
- What did you `grep` for, and in what file(s)?
- Any `find` commands you ran — what were you hunting for, and what came back:

## Getting help
- Did you actually use `man`/`--help`/`whatis` on anything, or figure it out by trial and error?

## Wildcards / globbing
- Any wildcard commands you ran (`*.txt`, `rm something-*`, etc.) — and did globbing ever surprise you?

## Streams and exit status
- Did you use `2>/dev/null`, `2>&1`, or check `$?` anywhere? Actual commands:

## Archiving
- Any `tar`/`zip` commands you ran — what were you archiving/extracting?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
