<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/networking-core-protocols/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# Networking Core Protocols — raw notes

## Target / environment
- Target IP or hostname (if any lab machine was involved):
- Attack box used (TryHackMe AttackBox / own Kali / other):

## Recon / packet inspection
- Tools used (tcpdump, Wireshark, ping, traceroute, etc.) and the actual command(s) you ran:
- What you were looking at (a TCP handshake, a UDP exchange, an ICMP ping/traceroute)?
- What the output actually showed (SYN/SYN-ACK/ACK sequence, flags, TTLs, etc.):
- Anything that stood out (unexpected RST, blocked ICMP, filtered vs closed port, etc.):

## Header-level details you worked through
- TCP flags/handshake — did the room have you identify SYN/ACK/FIN/RST in a capture or diagram?
- UDP — what example (DNS, DHCP, etc.) did the room use to show connectionless delivery?
- ICMP — did the room have you run ping/traceroute and interpret Echo Request/Reply or Time Exceeded
  messages?

## Wrong turns
- Anything you tried that didn't pan out, or a misread field/flag that confused you at first:

## Flags / answers
- Any flags you captured (I'll redact the actual values in the published article — just note
  where/how you got each one):
- Any other task answers worth remembering:

## What took longest / what you'd do differently
-

## Anything else worth noting
-
