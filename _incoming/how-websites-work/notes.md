<!--
Fill this in from memory. Rough bullets are fine — I turn this into full prose. This room is
concept/quiz-style rather than hands-on exploitation, so the notes lean toward what you learned and
how you'd apply it, not commands/output. Delete any section that doesn't apply.

Drop any screenshots you have into this same folder (_incoming/how-websites-work/) — filenames
don't matter. No screenshots is fine too.
-->

# How Websites Work — raw notes

## What the room actually covered (in your own words)
- The end-to-end pipeline (DNS → TCP → TLS → HTTP → render) — how did the room walk through it?
- DNS resolution step — what level of detail did the room go into here vs. pointing to a dedicated
  room?
- TCP/TLS handshake step — did the room diagram this, or just describe it at a high level?
- HTTP request/response step — what example request/response did the room use?
- Parsing and rendering (DOM/CSSOM/render tree) — how deep did the room go into browser internals?

## Anything the room got you thinking about beyond the textbook definition
- e.g. which stage of the pipeline a given attack (DNS spoofing, MITM, XSS) actually lives in:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can
  stay visible in the final article):

## Where this connects to other rooms you've done
- e.g. does this tie into DNS in Detail, HTTP in Detail, Putting it all together?

## What took longest / what surprised you
-

## Anything else worth noting
-
