<!--
Fill this in from memory. Rough bullets are fine — I turn this into full prose. This room is
concept/quiz-style rather than hands-on exploitation, so the notes lean toward what you learned and
how you'd apply it, not commands/output. Delete any section that doesn't apply.

Drop any screenshots you have into this same folder (_incoming/osi-model/) — filenames
don't matter. No screenshots is fine too.
-->

# OSI Model — raw notes

## What the room actually covered (in your own words)
- The seven layers — did the room use a mnemonic, a diagram, or examples per layer?
- Encapsulation/decapsulation — how did the room illustrate data moving down and back up the stack?
- OSI vs TCP/IP model — did the room explicitly compare the two, or focus only on OSI?
- Per-layer security relevance (e.g. Layer 2 ARP spoofing, Layer 3 IP spoofing, Layer 7 SQLi) — which
  layer-to-attack mappings did the room call out?
- Devices mapped to layers (hub/switch/router/firewall/WAF) — any examples the room used?

## Anything the room got you thinking about beyond the textbook definition
- e.g. a case where OSI's clean layering didn't match a real protocol (like TLS spanning layers), or
  a "which layer is this attack happening at" moment:

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can
  stay visible in the final article):

## Where this connects to other rooms you've done
- e.g. does this tie into What is Networking?, Intro to LAN, Packets & Frames?

## What took longest / what surprised you
-

## Anything else worth noting
-
