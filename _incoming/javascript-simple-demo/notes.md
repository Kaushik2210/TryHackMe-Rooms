<!--
Fill this in from memory / your browser console history. Rough bullets are fine — I turn this into
full prose. The more specific you are (actual code, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/javascript-simple-demo/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# JavaScript: Simple Demo — raw notes

## Environment
- Where did you run/edit the JavaScript (browser dev console, an in-room code editor, Node.js locally)?

## Variables, types, functions
- Any actual `let`/`const`/`var` or function code you wrote or were given — what did it do?
- Did you hit the `==` vs `===` coercion gotcha, or anything else type-related that surprised you?

## Loops and arrays
- Any `for...of` / `forEach` code you actually ran — on what data?

## DOM and event handling
- What page/element did you interact with? What did the JavaScript actually do on click/input?
- Was there a client-side-only validation example (e.g. a password length check) — what did it look like, and did you try to bypass it?

## Client-side trust boundary
- Did you actually demonstrate bypassing a client-side check (disabling JS, editing the DOM, sending a raw request)? What did you use — dev tools, curl, Burp?

## Node.js (if covered)
- Did you run anything with `node`/`console.log` outside the browser? What?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
