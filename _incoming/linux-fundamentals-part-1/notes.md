<!--
Fill this in from memory / your terminal history. Rough bullets are fine — I turn this into full
prose. The more specific you are (actual commands, actual output, actual wrong turns), the better
and more defensible the final article. Delete any section that doesn't apply. Don't worry about
grammar or completeness; I'll ask follow-up questions for anything that's too thin to write from.

Drop any screenshots you have into this same folder (_incoming/linux-fundamentals-part-1/) —
filenames don't matter, I'll rename them. No screenshots is fine too.
-->

# Linux Fundamentals Part 1 — raw notes

## Target / environment
- Attack box or lab machine used, and how you connected (SSH / browser terminal):

## First commands
- What did `echo`, `whoami` actually show you when you ran them?
- Any early confusion about what a "shell prompt" was doing?

## Navigation
- `pwd`/`ls`/`cd` — what did your home directory actually contain, and where did you explore?
- `ls -l` / `ls -la` — anything in the long listing that stood out (owners, hidden files)?

## grep (first pass)
- What did you search for with `grep`, and in what file?

## Chaining commands
- Any `&&`, `;`, or `|` combos you actually typed:

## Tab completion and history
- Did tab completion save you from a typo? Any `history`/`Ctrl+R` moments worth noting?

## sudo
- Did you run any `sudo` commands? What happened (password prompt, permission granted/denied)?

## Command syntax (options vs. arguments)
- Any command where the options/arguments distinction tripped you up?

## Task answers
- Any specific answers you had to submit (I'll redact flag-style values, but conceptual answers can stay visible in the final article):

## What took longest / what you'd do differently
-

## Anything else worth noting
-
